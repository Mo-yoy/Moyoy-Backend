spring:
  config:
    import:
      - "aws-parameterstore:/config/moyoy-test/"

  jpa:
    hibernate:
      ddl-auto: update


aws:
  parameterstore:
    enabled: true
    prefix: /config
    name: moyoy-test